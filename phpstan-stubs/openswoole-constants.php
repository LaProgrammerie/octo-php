<?php

declare(strict_types=1);

/**
 * OpenSwoole constant stubs for PHPStan.
 *
 * Loaded via bootstrapFiles to define constants that the extension
 * references in default parameter values but doesn't expose to PHP.
 */

if (!defined('ITIMER_REAL')) {
    define('ITIMER_REAL', 0);
}
if (!defined('ITIMER_VIRTUAL')) {
    define('ITIMER_VIRTUAL', 1);
}
if (!defined('ITIMER_PROF')) {
    define('ITIMER_PROF', 2);
}
if (!defined('SWOOLE_HOOK_ALL')) {
    define('SWOOLE_HOOK_ALL', 0x7FFFFFFF);
}
if (!defined('SWOOLE_HOOK_CURL')) {
    define('SWOOLE_HOOK_CURL', 1 << 12);
}
